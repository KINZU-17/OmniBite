{
  "name": "@omnibite/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared API contract: socket events, room helpers, and status types used by the OmniBite backend and frontends.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
